name: my_project

packages:
  - feature/**
  - packages/**
  - shared/**

scripts:
  analyze: flutter analyze

  test: flutter test

  ci:
    run: |
      melos run test
      melos run analyze

  build:web:
    run: |
      melos bs
      cd apps/app
      flutter build web --release

  format: dart format .
